on:
  push:
    tags:
      - gateway-v*

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - checkout
      - docker build -t authgate-gateway:${{ tag }} .
      - docker push authgate-gateway:${{ tag }}
